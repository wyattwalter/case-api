apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: case-api
  description: A silly API no one should use.
  annotations:
    argocd/app-name: kong
spec:
  type: service
  lifecycle: production
  owner: user:wyattwalter
  providesApis:
    - case-api
